# R4.21

Run:

```powershell
.\run_v0_6D1_R4_21.ps1
```

Expected behavior: static validation and a frozen symmetric reexecution authorization registry only. No external engine is launched.

Outputs are written under `outputs/v0_6D1_R4_21` and the fail-closed seal under `outputs/v0_6D1_R4_21_SEAL`.
