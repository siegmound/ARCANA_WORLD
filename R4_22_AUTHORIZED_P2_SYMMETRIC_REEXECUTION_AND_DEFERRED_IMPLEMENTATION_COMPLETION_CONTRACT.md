# v0.6D1-R4.22 — Authorized P2 Symmetric Reexecution & Deferred Implementation Completion

## Purpose
Execute only the exact 11 frozen R4.2 jobs authorized and sealed by R4.21, using the exact R4.21 candidate adapter source hashes and the exact R4.3 replicate seeds. Audit the deferred Geonomics spatial-binding problem without inventing scalar-to-raster inputs.

## Authorized execution scope
R4.22 may execute only the R4.21 authorization registry:
- CDMetaPOP: all 5 frozen R4.2 CDMetaPOP jobs;
- NEMO: all 3 frozen R4.2 NEMO jobs;
- SLiM: all 3 frozen R4.2 SLiM jobs.

No Madingley, RangeShiftR, Geonomics, P3 extension job, target-design execution, readjudication, canonical replay, or canonical parameter change is authorized in R4.22.

## Exact-source / exact-seed rule
The executable adapter is the source file that R4.21 statically validated. R4.22 verifies its SHA256 against R4.21 before execution. Every replicate must reuse the R4.3 `JOB_CONTRACT.engine_input.replicates` `(replicate_index, seed)` ledger exactly.

## CDMetaPOP semantics
R4.22 uses the R4.21 matched dynamic/neutral adapter and the frozen R4.7 repair profile for all five CDMetaPOP jobs. `population_response_ratio` remains `PROXY_ONLY`; only the matched dynamic/neutral effect is retained as a diagnostic/domain candidate. No comparison target is consumed by the adapter.

## NEMO semantics
The qfreq extractor is retained as raw allele-frequency evidence only. Expected heterozygosity is not additive genetic variance. Allele-frequency differentiation is not a gene-flow rate.

## SLiM semantics
Tree-sequence diversity is not ancestry contribution. FST is not migration rate. Sample/population/time semantics remain explicit.

## Deferred Geonomics completion
R4.22 audits canonical spatial sources without running Geonomics. R3.28 provides an explicit 200 ka→0 spatial-deme trajectory; R3.33/R3.34 provide explicit 20 ka→0 gridded resource landscapes. The 3 Ma→200 ka frozen Geonomics window still lacks an explicit spatial state covering the window start: R3.27 is macro/non-spatial and R3.28 begins at the 200 ka boundary. Therefore the full shared Geonomics adapter remains deferred. Scalar-to-raster synthesis and `run_default_model()` as historical evidence are forbidden.

## Fail-closed completion
R4.22 seals only if all 11 authorized jobs produce PASS raw evidence, all exact seed ledgers match, all engine-specific semantic guards pass, target leakage is absent, canonical writes are absent, and the deferred Geonomics audit remains non-executing.

## Next stage
On success: `BUILD_R423_P2_REEXECUTION_EVIDENCE_NORMALIZATION_AND_GEONOMICS_CANONICAL_SPATIAL_BINDING_COMPLETION`.
