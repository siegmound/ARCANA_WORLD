# ARCANA WorldSim v0.6D1-R4.34
## Geonomics Runtime Selector Authority Freeze & Native Parameter Materialization Preflight

**Status:** CANDIDATE OVERLAY — requires live execution on the authoritative Windows root.

### Required parent

R4.33 must be SEALED with:
- 57 target-binding records statically adjudicated;
- 4 exact-source future execution candidates + 1 source-identity authority gap;
- 39 catalog rules deferred;
- 12 observable exact-source bindings deferred;
- 1 primary mapping authority frozen;
- exactly 3 Geonomics runtime binding packages compiled and hash-bound;
- selector authority required for all 3;
- native params = 0;
- make_model = not performed;
- no Geonomics execution / numeric target execution / readjudication / canonical write.

### What R4.34 freezes

R4.34 does NOT choose the "best" candidate.

It freezes a pre-result selector policy:
- canonical time axes -> preserve the full ordered axis;
- candidate/member/forcing/taxon dimensions -> exhaustive enumeration of every frozen member;
- J21 environment/producer layer sets -> preserve all layers as separate provenance channels;
- J21 cross-layer combination -> no numerical fusion.

This creates reproducible selector authority without target leakage.

### Why R4.34 still does not write a run-capable Geonomics params file

Geonomics native parameters require semantic choices that R4.33 did not authorize:
- landscape dim/res/ulc/prj geometry;
- conversion of physical age axes to integer model timesteps;
- exact frozen seed binding to model.num;
- scaling/normalization of layer values;
- J14/J18 ARCANA population -> literal Geonomics individual representation;
- exact spatial initial-state injection instead of random K-weighted initialization;
- J14/J18 K_layer / K_factor role;
- J21 resource/environment -> engine layer-role binding.

Using defaults here would be a new hidden model assumption.

Therefore a successful R4.34 is expected to:
- freeze selector authority for all 3 jobs;
- complete all 3 native-parameter preflights;
- explicitly enumerate the remaining authority gaps;
- materialize 0 native Geonomics parameter files;
- call neither read_parameters_file nor make_model;
- execute no external engine.

### Next natural stage

`BUILD_R435_GEONOMICS_NATIVE_SCHEMA_MAPPING_INITIAL_STATE_ADAPTER_AND_SEED_AUTHORITY_CLOSURE`

R4.35 can close the exact schema/unit/seed/initial-state adapter authorities before any
native model construction.

### Run

```powershell
.\run_v0_6D1_R4_34.ps1
```
