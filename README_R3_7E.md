# R3.7E

First World-1 adaptive-genetic shadow replay using the R3.7D K envelope. The full governed run is 210→205 Ma and is intentionally executed locally because it replays the parent twice to prove canonical parity. A 210→209 diagnostic smoke is included and verified in-package.

Run `run_v0_6D1_R3_7E_short_shadow.ps1` for the full gate. Results are written to `local_runs/v0_6D1_R3_7E`.
