# R3.14 Next-stage handoff

## If local R3.14 PASSes

Return the console output and zip only:

`local_bindings\v0_6D1_R3_14\`

The next step is to audit/reseal R3.14 and then build:

`v0.6D1-R3.15 — Late-Cenozoic H0 Adaptive Biological Replay Integration`

R3.15 must explicitly solve the coupling between the variable environmental C2 clock and the currently fixed 125 kyr R3.7I/R3.8 biological cadence. It must not reinterpret the scheduler as an automatic variable biological timestep.

## If local R3.14 is BLOCKED

The R3.13 biological boundary remains fully SEALED and valid at 30 Ma. Supply an additional archive search root and rerun R3.14. The blocked report lists the exact required SHA-256 values.
