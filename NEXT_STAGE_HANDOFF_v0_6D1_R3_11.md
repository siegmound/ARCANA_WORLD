# NEXT STAGE HANDOFF — v0.6D1-R3.11

R3.11 candidate is prepared from R3.10 SEALED.

Canonical production run still required:

```powershell
.\run_v0_6D1_R3_11_postcha1_recovery.ps1
```

Target:

```text
65.5 -> 61.0 Ma
36 biology steps
```

After the local result is returned:

1. audit all 36 telemetry records;
2. verify zero q-ceiling contacts;
3. distinguish initial pre-CHA1 founder carry-over speciations from other
   post-restart speciations;
4. verify no CHA-1 reapplication and Deep OFF;
5. verify 61 Ma serialization/restart identity;
6. run inherited regression;
7. reseal R3.11.

Only after R3.11 is SEALED should the next long recovery interval be opened.
A natural subsequent stage is the continuation of post-CHA1 diversity recovery
from the +5 Myr boundary toward the later recovery horizon, using the same
production runtime and without a global diversification target.
