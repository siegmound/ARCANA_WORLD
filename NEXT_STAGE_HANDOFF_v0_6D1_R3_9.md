# Next-stage handoff after R3.9

If the full R3.9 150→66 Ma run passes and the PRE_CHA1 checkpoint is sealed, the next stage is a dedicated **CHA-1 high-resolution event bridge**.

The bridge must begin from the exact 66.0 Ma PRE_IMPACT checkpoint and apply the already-audited CHA-1 event at event-relative time `t=0`, preserving the distinction:

`66.0 Ma PRE_IMPACT (pulse=0) -> CHA-1 discontinuity/high-resolution response -> post-impact state`.

Do not represent CHA-1 as one ordinary 125 kyr biology step. H0 keeps Deep biological coupling OFF; HX will later reuse the same physical event with its separately governed Deep coupling path for causal comparison.
