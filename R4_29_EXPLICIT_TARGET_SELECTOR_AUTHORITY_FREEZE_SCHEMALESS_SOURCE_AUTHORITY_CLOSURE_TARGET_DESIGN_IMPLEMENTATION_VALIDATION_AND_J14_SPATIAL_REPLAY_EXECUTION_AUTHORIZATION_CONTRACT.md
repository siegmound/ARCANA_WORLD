# ARCANA WorldSim — v0.6D1-R4.29 Contract

## Explicit Target Selector Authority Freeze, Schemaless Source Authority Closure, Target-Design Implementation Validation & J14 Spatial Replay Execution Authorization

### Parent authority
Requires v0.6D1-R4.28-R1 SEALED with exactly 44 terminal selector preflights: 38 exact-inventory ready, 6 explicitly deferred without exact selector inventory, 0 selector authorizations; 12 nonnumeric target-design authority definitions; one semantic-repair preflight; and J14 spatial-replay authority preflight ready.

### Selector authority
R4.29 may freeze a selector only from a **pre-result exact selector rule catalogue** and only when the rule binds uniquely to the exact source inventory. Full-selector equality is preferred; JSON may bind a unique exact leaf token to its full frozen path. Fuzzy matching, synonym fallback, substring matching, outcome-driven selection and external-engine comparison are forbidden. Multiple exact matches are deferred, never tie-broken from results.

### Schemaless closure
The six R4.28 schemaless cases are re-opened read-only. JSON dict paths, NPZ member names and tabular headers may be recovered only after source hash verification. JSON list-root inference and selector synthesis are forbidden. Unrecoverable cases become an explicit source-authority gap.

### Target-design validation
All 12 R4.28 definitions must exactly preserve the R4.26 request and R4.27 adjudication: scientific construct, observables, unit families, governance and forbidden shortcuts. Validation remains nonnumeric.

### Semantic repair
The single R4.18 rejection-cause repair remains limited to `eligible_primary_row_count` / primary-mapping eligibility review. Numeric target value, mapping class and readjudication remain immutable in R4.29.

### J14 new spatial authority
R4.29 statically authorizes execution of a new **engine-independent** spatial reconstruction authority only if the frozen implementation and canonical inputs validate. The construction is `MINIMUM_DISPLACEMENT_MAXIMUM_ENTROPY_SPATIALIZATION`:
- spatial support comes from the canonical late-Cenozoic A1 land-support provider;
- R3.27 supplies only effective-population and deme-count constraints;
- initialization is maximum entropy over age-matched canonical land support;
- existing deme cells persist while accessible;
- support loss uses minimum-distance remap;
- fission uses deterministic farthest-point placement;
- population is maximum-entropy equal share across active demes.

R3.15 and R3.28 spatial coordinates are forbidden construction inputs. R3.28 may later be a withheld diagnostic only. Geonomics is not executed or authorized by R4.29 and R4.2/J14 is immutable.

### Governance
- engine execution: none
- numeric target execution: none
- canonical spatial replay execution: none in R4.29
- readjudication: none
- canonical state/parameters: unchanged
- Deep biological coupling: OFF
- majority vote: forbidden

### Next action
`BUILD_R430_AUTHORIZED_TARGET_MATERIALIZATION_SELECTOR_GAP_CLOSURE_AND_J14_SPATIAL_REPLAY_AUTHORITY_EXECUTION`
