# v0.6D1-R4.20 — P2 Adapter Implementation Preflight & Target-Design Protocol Repair

R4.20 consumes the SEALED R4.19 execution-plan freeze. It does **not** execute an external engine. It builds an engine-specific enhancement layer contract for each of the six P2 backlog cells and freezes result-independent target-protocol repairs for the 57 active target backlog records.

## P2 implementation preflight

Each R4.19 P2 record must resolve to CDMetaPOP, Geonomics, NEMO, or SLiM, retain the exact symmetric frozen R4.2 job scope, and bind to at least one real historical adapter source in the project. Historical adapters are never overwritten; implementation proceeds in a new namespace.

Engine-specific invariants include:
- **Geonomics:** default/example landscape cannot be adjudicative; a canonical ARCANA landscape binding with explicit spatial/temporal provenance is required.
- **CDMetaPOP:** corrected `summary_popAllTime.csv` lifecycle semantics remain authoritative for population extraction; absolute population response remains PROXY_ONLY after R4.12; matched-control semantics require an explicit predeclared protocol.
- **NEMO:** qfreq-derived statistics must preserve locus/population/time semantics; heterozygosity is not additive variance and frequency differentiation is not migration rate without an explicit causal mapping.
- **SLiM:** tree-sequence metrics require explicit sample/population/time semantics; diversity is not ancestry and FST is not migration rate by identity.

No symmetric reexecution is authorized in R4.20. R4.21 must statically validate the concrete new-namespace implementation against the discovered parent adapter sources before reexecution can be separately authorized.

## Target protocol repair

The 57 active records are frozen into explicit repair dispositions. R4.20 never invents a numeric target. The 12 no-source target-design gaps receive scientific-construct/source-authority requirements only. The single R4.18-rejected materialization preserves the exact failed validation gates and forbids changing its numeric value merely to obtain concordance. The two PROXY_ONLY materializations remain context-only.

## Governance

No majority vote, no target leakage, no result-selected transform, no synthetic scientific evidence, no canonical replay or parameter change, and Deep biological coupling remains OFF.
