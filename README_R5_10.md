# R5.10 — R5.9 ↔ R3.29 Settlement/Cultural-Precondition Reconciliation

Extract as an overlay on the current post-R5.9 WorldSim root and run:

```powershell
.\run_v0_6D1_R5_10.ps1
```

R5.10 does not rerun R3.29 or any external engine. It verifies the sealed 50→0 ka community/precondition layer against the newer reconciled 0 ka handoff, preserves both candidate lineages, and keeps R3.29 robustness priority strictly diagnostic.
