# ARCANA WorldSim v0.6D1-R5.0

Substantive stage: selective high-resolution nested replay and arbitrary-age state query.

Implemented and sealed capability:
- query contract for arbitrary ages/optional native-grid region;
- per-product authority resolution;
- exact 20 ka demonstration;
- bounded 17.5 ka demonstration;
- deterministic derived-state packaging with provenance and uncertainty;
- strict final seal binding R4.56 runtime closure, 9 immutable scientific parent hashes, 15 candidate artifacts, semantic NPZ hashes and replay recipes.

Run from the authoritative post-R4.56 repository root:

```powershell
.\run_v0_6D1_R5_0.ps1
```

Expected final line after a valid live run:

`PASS_R50_INTEGRATED_AND_FINAL_SEAL_RUN`

R5.0 derived states are not automatic canonical rewrites.
