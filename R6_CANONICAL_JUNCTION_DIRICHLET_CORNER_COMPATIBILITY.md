# Canonical junction Dirichlet corner compatibility

Corner records: 240; expected 240. Counts: `{'CORNER_COMPATIBLE': 54, 'CORNER_NUMERICALLY_UNRESOLVED': 155, 'CORNER_DIRICHLET_CONFLICT': 31}`. Each evaluated record compares the two port-implied vectors at the same spherical point and records the expected pure shared-plate limit. Conflicting values are never averaged. Non-evaluated corners remain explicitly unresolved.

