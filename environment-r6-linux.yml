name: arcana-r6-linux
channels:
  - conda-forge
dependencies:
  - python=3.12.14
  - numpy
  - pytest
  # Candidate bounded topology backend only; R6 canonical semantics remain backend-independent.
  - pygplates=1.0.0
