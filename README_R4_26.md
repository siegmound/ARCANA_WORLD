# R4.26 overlay

Apply this overlay to the SEALED World1 root after R4.25, then run:

```powershell
.\run_v0_6D1_R4_26.ps1
```

R4.26 performs no external-engine run and no target numeric materialization. It implements hash-bound read-only target extractor infrastructure for the 44 R4.25 canonical-extractor-ready cells, freezes the single cause-only semantic repair implementation, freezes 12 target-design authority requests, and freezes the Geonomics J14 new spatial-authority request.

Expected next stage after a successful seal:
`BUILD_R427_TARGET_EXTRACTOR_STATIC_VALIDATION_AND_TARGET_DESIGN_GEONOMICS_J14_AUTHORITY_REQUEST_ADJUDICATION`.
