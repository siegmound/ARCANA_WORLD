# v0.6D1-R5.7 — R5.3–R5.6 Integrated Scientific Closure

R5.7 is a closure stage, not a new scientific simulation stage.

It seals one coherent evidence block:

- R5.3 — CDMetaPOP 3.08 demographic persistence / bottleneck diagnostics.
- R5.4 — NEMO 2.4.2 matched FLOW / no-flow genetic robustness diagnostics.
- R5.5 — ARCANA-native cross-lineage contact-opportunity history.
- R5.6 — SLiM 5.2 + tskit 1.0.3 targeted ancestry/admixture challenges.

## Closure rules

1. R5.2 remains the last pre-block sealed scientific parent and must match its exact sealed hashes.
2. R5.3–R5.6 remain candidate artifacts; no retrospective micro-seals are created.
3. Every candidate output manifest is re-hashed against the live files.
4. Raw external-engine evidence manifests from R5.3, R5.4 and R5.6 are re-hashed file-by-file.
5. Candidate-to-candidate hash bindings are recalculated live.
6. Cross-stage non-circularity is revalidated: R5.3 does not promote R5.2 RangeShiftR geometry; R5.4 does not fit NEMO to CDMetaPOP; R5.5 contact geometry is ARCANA-native; R5.6 uses only the binary R5.5 contact schedule as contact authority and does not convert contact-support fractions into migration rates.
7. No new external runtime is executed in R5.7.
8. The seal certifies evidence provenance, integrity and governance semantics. It does not claim a unique literal demographic history, numeric gene-flow truth, or realized historical admixture fraction.
9. Canonical state is unchanged and Deep biological coupling remains OFF.

## Observed block facts bound by this closure

- R5.3: 12 families, 36 groups, 72 CDMetaPOP streams.
- R5.4: 36 groups, 72 matched FLOW/control pairs, 144 NEMO streams.
- R5.5: 35 cross-lineage pairs across 141 age states and 105 heterogeneous context records.
- R5.6: the 35 pair contact histories deduplicate to one binary schedule class; 3 standardized variants × 2 seeds = 6 SLiM streams.

After a successful R5.7 seal, the next scientific work should reconcile the sealed block with the full 3 Ma→200 ka hominid history and the `HUMAN_200KA = RPT_010_D02 + RPT_009_D02` cohort, rather than reopening R5.3–R5.6.
