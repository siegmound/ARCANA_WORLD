# Handoff — v0.6D1-R2.1 → v0.6D1-R3

## Completed authority

R2.1 closes the immediate runtime blocker left by R2:

- governed speciation birth actuator;
- deterministic ordinary-extinction actuator;
- exact D3.0C structural-gate reuse;
- D3.2B founder viability reuse;
- fixed internal biology/transport cadence independent of external chunking;
- synthetic validation of speciation, extinction and demographic fission;
- full H0 210→180 Ma replay with no forced events and <0.011% population error vs A1.

Persistent-vicariance fission is implemented but production-disabled because the R2/R2.1 self-contained environment only resolves paleogeographic support at 210 and 180 Ma.

## Next stage

`v0.6D1-R3 — Rebased Natural-Control Deep-Time Barrier-History Binding & Long-Horizon H0 Production Replay`

### Primary objectives

1. Bind the full surviving deep-time environmental/reference sequence rather than the two-keyframe 210/180 pilot surface.
2. Construct/verify an event-aware paleogeographic barrier-history provider using existing ARCANA providers/libraries before introducing custom algorithms.
3. Re-test persistent-vicariance fission against resolved physical barriers and enable it only if false fragmentation is eliminated or bounded.
4. Preserve R2.1 internal cadence semantics and external-chunk bit-exactness.
5. Add checkpoint/resume over long windows and prove resumed state/event identity.
6. Extend H0 in validated windows toward CHA-1, with explicit high-resolution handoff around ~66 Ma instead of smearing the catastrophe over a deep-time macrostep.
7. Continue using the old D2/D2.2 history only as a reference envelope, never as a forced target.
8. Keep Deep biological coupling OFF until H0 is fully validated.

### Recommended execution sequence

- R3A: full deep-time environmental/barrier provider binding + audit;
- R3B: persistent-vicariance activation sensitivity;
- R3C: H0 210→150 Ma / 150→90 Ma long-horizon checkpointed pilots;
- R3D: pre-CHA1 90→70 Ma validation;
- R3E: event-aware CHA-1 handoff and post-event continuation.

Only after H0 reaches a validated 210→0 Ma production history should the paired HX replay be authorized from the same R1 common state.
