# R4.6-R1 Authorized-Case Scope Repair

The initial R4.6 candidate blocked at its pre-execution scope gate. It expected RangeShiftR/J08, while the SEALED R4.5 diagnosis authorized CDMetaPOP/J09 as the single robust PRIMARY `population_persistence` row in `H0_POST_CHA1_RECOVERY`.

This repair:
- changes no R4.5 output;
- changes no R3/R4 canonical parameter;
- preserves the blocked R4.6 audit via runner repair history;
- targets the exact parent-authorized CDMetaPOP/J09 case;
- audits the actual common CDMetaPOP R4.3 adapter;
- keeps the same R3.11 diagnostic reference-population-hold intervention;
- requires symmetric treatment of all five R4.3 CDMetaPOP jobs if adapter semantics change.

The initial blocked R4.6 produced no counterfactual scientific result, so this is a control/scope repair rather than result replacement.
