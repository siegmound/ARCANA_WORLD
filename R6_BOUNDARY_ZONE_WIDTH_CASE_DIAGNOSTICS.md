# Boundary-zone width case diagnostics V4

Evaluated widths: `[100000, 250000, 500000, 1000000]`. Not evaluated: `[]`. No status is inferred for omitted widths; global L0/L1 is not run unless an evaluated width passes its all-20 local gate.

