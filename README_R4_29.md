# v0.6D1-R4.29

This overlay consumes the SEALED R4.28-R1 outputs and performs four fail-closed tasks: exact pre-result selector authority freeze, read-only closure of six schemaless source cases, exact validation of twelve nonnumeric target-design definitions, and static authorization of the new engine-independent J14 3 Ma→200 ka spatial replay authority.

R4.29 does **not** calculate numeric targets, execute the spatial replay, run Geonomics, readjudicate the 75-cell matrix, mutate R4.2, or change canonical state.

Run from the WorldSim root:

```powershell
.\run_v0_6D1_R4_29.ps1
```

Main outputs are under `outputs/v0_6D1_R4_29/`; the fail-closed seal is `outputs/v0_6D1_R4_29_SEAL/R4_29_FINAL_SEAL_AUDIT.json`.
